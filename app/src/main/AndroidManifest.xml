<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tw.edu.pu.csim.tcyang.android2dgame">

    <application
        android:allowBackup="true"
        android:icon="@drawable/virus1"
        android:label="@string/app_name"
        android:roundIcon="@drawable/virus1"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android2DGame">
        <activity android:name=".OverActivity" android:screenOrientation="landscape" />
        <activity android:name=".GameActivity" android:screenOrientation="landscape" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>